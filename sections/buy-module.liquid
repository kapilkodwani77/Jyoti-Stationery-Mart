{{ 'component-buy-box.css' | asset_url | stylesheet_tag }}
<section class="sec sec--tight">
  <div class="wrap" style="max-width:560px">
    {% render 'buy-box', product: settings.featured_product, compact: true %}
  </div>
</section>

{% schema %}
{ "name": "Buy module" }
{% endschema %}
