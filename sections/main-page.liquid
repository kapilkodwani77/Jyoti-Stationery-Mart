<section class="sec">
  <div class="wrap" style="max-width:720px">
    <h1 class="h2" style="margin-bottom:var(--s-6)">{{ page.title }}</h1>
    <div class="body-copy page-content">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{ "name": "Page content" }
{% endschema %}